import { AudioRecordingModule } from '../modules/audio-recording/audio-recording.module';
import { CommandModule } from '../modules/commands/command.module';

export const APP_MODULES = [AudioRecordingModule, CommandModule];
